server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/expensetracker
    username: snappay
    password: snappay
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: "87CBD112DEABA93FECA4A959ADB9B87CBD112DEABA93FECA4A959ADB9B"
  expiration-ms: 3600000 # 1 hour

logging:
  level:
    org.springframework.security: DEBUG